import './bootstrap';

//import Alpine from 'alpinejs';

//window.Alpine = Alpine;

// Está comentado porque livewire ya inicializa alpinejs, si se quita
//el comentario se intentará iniciarlo 2 veces y las funcionalidades de livewire no serán aplicadas
//Alpine.start();
